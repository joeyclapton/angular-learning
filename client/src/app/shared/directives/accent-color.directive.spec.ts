import { AccentColorDirective } from './accent-color.directive';

describe('AccentColorDirective', () => {
  it('should create an instance', () => {
    const directive = new AccentColorDirective();
    expect(directive).toBeTruthy();
  });
});
